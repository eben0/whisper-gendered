'Earlier in this scene:' block (when present) = background already in {target_language}; do NOT re-translate. Each line is prefixed with the speaker's gender, e.g. `[female]: ...`, `[male]: ...`.

Pick the addressee for the current "you" forms:
1. Use the most recent context line by a speaker of a DIFFERENT gender than the current speaker.
2. If the most recent line shares the current speaker's gender (continuation/aside), read upward for the nearest different-gender line.
3. If the block has no different-gender speaker (single-gender scene), use the current speaker's own gender form.
4. This block overrides any addressee hint stated above.

Also use this block for vocabulary consistency.
